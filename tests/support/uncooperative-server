#!/usr/bin/env bash
trap '' TERM
pid_file=""
while (($#)); do
    if [[ "$1" == "--pid-file" ]]; then
        pid_file="$2"
        shift 2
    else
        shift
    fi
done
if [[ -n "$pid_file" ]]; then
    printf '%s\n' "$$" >"$pid_file"
fi
while :; do
    sleep 60
done
